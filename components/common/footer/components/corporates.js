import React from "react"
import styles from "../styles/corporates.module.css"

function corporates() {
  return (
    <div className={styles.footerCorporates}>
      <div className={`${styles.toLeft} ${styles.boxWrapper}`}>
        협찬회사들(슬라이드)
      </div>
      <div className={`${styles.toLeft} ${styles.boxWrapper}`}>
        협찬회사들(슬라이드)
      </div>
      <div className={`${styles.toLeft} ${styles.boxWrapper}`}>
        협찬회사들(슬라이드)
      </div>
    </div>
  )
}

export default corporates
